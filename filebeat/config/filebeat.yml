filebeat:
    prospectors:
        -
            paths:
                - /opt/oneweb/services/tomcat/tomcat.log
            input_type: tomcat
            document_type: tomcat


registry_file: /opt/oneweb/fb_registry.filebeat

output:
    logstash:
        hosts: ["logstash:5044"]
        worker: 1